<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="action_partner_project_list" model="ir.actions.act_window">
            <field name="name">Project Tags</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner.project</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a project Tag
              </p><p>
                Assign tags to your contacts to organize, filter and track them.
              </p>
            </field>
        </record>

        <menuitem 
        id="menu_partner_project_form"
        name="project Tags"
        parent="contacts.res_partner_menu_config"
        action="action_partner_project_list"
        groups="base.group_user"
        sequence="2"/>
         
    </data>
</odoo>